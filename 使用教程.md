# Ghost Agent 使用教程

> 你的数字替身 — 能自己操作电脑、浏览器、发推特，24小时自动工作

---

## 目录

1. [U盘安装（推荐）](#u盘安装推荐)
2. [Windows 安装](#windows-安装)
3. [macOS 安装](#macos-安装)
4. [配置 API Key](#配置-api-key)
5. [启动 Ghost](#启动-ghost)
6. [常用命令](#常用命令)
7. [常见问题](#常见问题)

---

## U盘安装（推荐）

### U盘里放什么

把这一个文件夹整个拷进 U 盘（**不需要** `.venv` 目录，可以删掉节省空间）：

```
NousResearch-hermes-agent-4117fc3\
```

U 盘里还需要带上你的配置文件（含 API Key）：
- `ghost_hermes.env` — 已在源码目录里，含所有 API Key
- `ghost_config.yaml` — 已在源码目录里，最大权限配置

---

## Windows 安装

### 第一步：把文件夹从 U 盘复制到电脑

比如放到 `D:\ghost\`

### 第二步：打开 PowerShell，允许运行脚本

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 第三步：运行安装脚本（需要联网）

```powershell
cd D:\ghost\NousResearch-hermes-agent-4117fc3
.\scripts\install-ghost-local.ps1
```

安装过程约 3-5 分钟，会自动：
- 安装 `uv`（Python 包管理器）
- 创建 Python 3.11 虚拟环境
- 安装所有依赖
- 创建 `ghost` 命令

### 第四步：复制配置文件

```powershell
New-Item -ItemType Directory -Path "$env:USERPROFILE\.hermes" -Force

Copy-Item "D:\ghost\NousResearch-hermes-agent-4117fc3\ghost_hermes.env" "$env:USERPROFILE\.hermes\.env"
Copy-Item "D:\ghost\NousResearch-hermes-agent-4117fc3\ghost_config.yaml" "$env:USERPROFILE\.hermes\config.yaml"
```

### 第五步：重开 PowerShell，启动

```powershell
ghost
```

### 可选：设置快捷命令 `g`

```powershell
New-Item -ItemType Directory -Path "$env:USERPROFILE\bin" -Force

@"
@echo off
cd /d "D:\ghost\NousResearch-hermes-agent-4117fc3"
.venv\Scripts\python.exe -m hermes_cli.main %*
"@ | Out-File "$env:USERPROFILE\bin\g.cmd" -Encoding ASCII

# 加入 PATH
$userPath = [Environment]::GetEnvironmentVariable("PATH", "User")
[Environment]::SetEnvironmentVariable("PATH", "$env:USERPROFILE\bin;$userPath", "User")
```

重开 PowerShell 后直接输入 `g` 启动。

---

## macOS 安装

### 第一步：把文件夹从 U 盘复制到 Mac

比如放到 `~/ghost/`

### 第二步：打开终端，运行安装脚本（需要联网）

```bash
cd ~/ghost/NousResearch-hermes-agent-4117fc3
bash scripts/install-ghost.sh
```

安装完成后重新加载终端：

```bash
source ~/.zshrc
```

### 第三步：复制配置文件

```bash
mkdir -p ~/.hermes
cp ghost_hermes.env ~/.hermes/.env
cp ghost_config.yaml ~/.hermes/config.yaml
```

### 第四步：开辅助功能权限（必须）

Ghost 要控制鼠标和键盘，需要开权限：

**系统设置 → 隐私与安全性 → 辅助功能 → 添加终端（Terminal 或 iTerm2）→ 打开开关**

### 第五步：启动

```bash
ghost
```

### 可选：设置快捷命令 `g`

```bash
echo 'alias g="cd ~/ghost/NousResearch-hermes-agent-4117fc3 && .venv/bin/python -m hermes_cli.main"' >> ~/.zshrc
source ~/.zshrc
```

---

## 配置 API Key

`ghost_hermes.env` 里已经有配置好的 API Key，复制到 `~/.hermes/.env` 就行。

如果需要修改：

**Windows：**
```powershell
notepad $env:USERPROFILE\.hermes\.env
```

**macOS：**
```bash
nano ~/.hermes/.env
```

### 推荐的免费 API Key

**Groq（完全免费，响应 < 1秒）：**
1. 打开 https://console.groq.com
2. 注册 → API Keys → Create API Key
3. 填到 `GROQ_API_KEY=` 后面

**阿里云百炼（国内稳定）：**
1. 打开 https://bailian.console.aliyun.com
2. API-KEY管理 → 创建API-KEY
3. 填到 `DASHSCOPE_API_KEY=` 后面

---

## 启动 Ghost

### 方式一：直接运行（推荐，用改好的代码）

**Windows：**
```powershell
cd D:\ghost\NousResearch-hermes-agent-4117fc3
.venv\Scripts\python.exe -m hermes_cli.main
```

**macOS：**
```bash
cd ~/ghost/NousResearch-hermes-agent-4117fc3
.venv/bin/python -m hermes_cli.main
```

### 方式二：用 ghost 命令

```
ghost
```

### 启动成功的样子

```
 ██████╗ ██╗  ██╗ ██████╗ ███████╗████████╗
██╔════╝ ██║  ██║██╔═══██╗██╔════╝╚══██╔══╝
██║  ███╗███████║██║   ██║███████╗   ██║
██║   ██║██╔══██║██║   ██║╚════██║   ██║
╚██████╔╝██║  ██║╚██████╔╝███████║   ██║
 ╚═════╝ ╚═╝  ╚═╝ ╚═════╝ ╚══════╝   ╚═╝

  your digital twin · full computer control

╔══════════════════════════════════════════╗
║ 👻 Ghost - Your Digital Twin             ║
║ Ghost v0.14.0 (2026.5.16)               ║
╚══════════════════════════════════════════╝

⚠ YOLO mode — all approval prompts bypassed

you>
```

看到 `⚠ YOLO mode` 说明最大权限已开启。

---

## 常用命令

### 在 Ghost 对话中

| 输入 | 说明 |
|------|------|
| `/model` | 切换 AI 模型 |
| `/skills` | 查看已学会的技能 |
| `/status` | 查看当前状态 |
| `/help` | 查看所有命令 |
| `Ctrl+C` | 退出 |

### 定时任务

```
ghost cron list                          # 查看所有定时任务
ghost cron add 名字 "0 9 * * *" "任务"   # 每天9点执行
ghost daemon                             # 启动后台守护进程
```

### Cron 表达式

```
"0 9 * * *"      每天早上9点
"0 9,18 * * *"   每天9点和18点
"0 */2 * * *"    每2小时
"0 9 * * 1"      每周一9点
```

---

## 常见问题

### Q：安装时提示"不允许运行脚本"

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q：pip 下载很慢

安装前设置国内镜像：
```powershell
$env:UV_INDEX_URL = "https://pypi.tuna.tsinghua.edu.cn/simple"
.\scripts\install-ghost-local.ps1
```

### Q：Playwright 浏览器下载失败

跳过浏览器，之后单独装：
```powershell
.\scripts\install-ghost-local.ps1 -SkipBrowser
```
之后：
```powershell
$env:PLAYWRIGHT_DOWNLOAD_HOST = "https://npmmirror.com/mirrors/playwright"
playwright install chromium
```

### Q：macOS 提示"无法控制鼠标"

系统设置 → 隐私与安全性 → 辅助功能 → 添加终端

### Q：遇到验证码

用真实 Chrome 模式：

**Windows：**
```
you> 用 shell_run 运行：Start-Process "C:\Program Files\Google\Chrome\Application\chrome.exe" -ArgumentList "--remote-debugging-port=9222"
```

**macOS：**
```
you> 用 shell_run 运行：open -a "Google Chrome" --args --remote-debugging-port=9222
```

然后：
```
you> 用 code_run 连接我的真实 Chrome（CDP 端口 9222），然后打开 x.com
```

### Q：新电脑安装后想同步最新代码

```powershell
# 如果有网络
git pull origin master

# 如果没网络，从 U 盘重新复制源码目录
```

---

## 联网需求说明

| 操作 | 需要联网 | 大小 |
|------|----------|------|
| 安装 uv | ✅ | ~10MB |
| pip install 依赖 | ✅ | ~200MB |
| Playwright Chromium | ✅ | ~150MB |
| 源码（从U盘） | ❌ | — |
| API Key 配置（从U盘） | ❌ | — |
| 日常使用 Ghost | ✅（调用 AI API） | — |

> 安装只需要联网一次，之后源码和配置都在本地，只有调用 AI 模型时需要联网。
