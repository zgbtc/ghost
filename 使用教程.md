# Ghost Agent 使用教程

> 你的数字替身 — 能自己操作电脑、浏览器、发推特，24小时自动工作

---

## Windows 安装

### 方式一：有 U 盘

1. 把文件夹从 U 盘复制到电脑，比如 `E:\openGhost\`
2. 双击 `安装.bat`（等 3-5 分钟）
3. 重开 PowerShell，输入 `g`

### 方式二：无 U 盘（从 GitHub 拉取）

```powershell
git clone https://github.com/zgbtc/ghost.git; cd ghost; powershell -ExecutionPolicy Bypass -File .\安装.ps1
```

重开 PowerShell，输入 `g` 启动。

---

## macOS 安装

### 方式一：有 U 盘

1. 把文件夹从 U 盘复制到 Mac，比如 `~/ghost/`
2. 打开终端运行：`bash ~/ghost/NousResearch-hermes-agent-4117fc3/安装.sh`
3. 开辅助功能权限（脚本会自动跳转设置）
4. 重开终端，输入 `g`

### 方式二：无 U 盘（从 GitHub 拉取）

```bash
git clone https://github.com/zgbtc/ghost.git && bash ghost/安装.sh
```

开辅助功能权限后，重开终端输入 `g` 启动。

---

## Ubuntu / Linux 安装

### 一行命令

```bash
git clone https://github.com/zgbtc/ghost.git && bash ghost/安装.sh
```

重开终端，输入 `g` 启动。

### 第三步：开辅助功能权限（必须，否则无法控制鼠标键盘）

脚本结束时会询问是否打开系统设置，选 Y 直接跳转。

手动路径：**系统设置 → 隐私与安全性 → 辅助功能 → 添加终端 → 打开开关**

### 第四步：重新打开终端，输入 `g` 启动

```
g
```

就这四步。

---

## 启动成功的样子

```
 ██████╗ ██╗  ██╗ ██████╗ ███████╗████████╗
██╔════╝ ██║  ██║██╔═══██╗██╔════╝╚══██╔══╝
...

  your digital twin · full computer control

╔══════════════════════════════════════════╗
║ 👻 Ghost - Your Digital Twin             ║
║ Ghost v0.14.0 (2026.5.16)               ║
╚══════════════════════════════════════════╝

⚠ YOLO mode — all approval prompts bypassed

you>
```

看到 `⚠ YOLO mode` 和 `you>` 就可以开始对话了。

---

## 配置 API Key

配置文件在 `ghost_hermes.env`，已经包含所有 API Key，安装时会自动复制。

如果需要修改（Windows）：
```powershell
notepad $env:USERPROFILE\.hermes\.env
```

如果需要修改（macOS）：
```bash
nano ~/.hermes/.env
```

### 推荐免费 API

| 服务 | 速度 | 获取地址 |
|------|------|----------|
| Groq | 最快，< 1秒 | https://console.groq.com |
| 阿里云百炼 | 国内稳定 | https://bailian.console.aliyun.com |
| 智谱 GLM | glm-4-flash 免费 | https://open.bigmodel.cn |

---

## 常用命令

| 输入 | 说明 |
|------|------|
| `g` | 启动 Ghost（Windows 快捷方式） |
| `ghost` | 启动 Ghost |
| `/model` | 切换 AI 模型 |
| `/skills` | 查看已学会的技能 |
| `/help` | 查看所有命令 |
| `Ctrl+C` | 退出 |

---

## 常见问题

**Q：双击 `安装.bat` 没反应**

右键 → 用 PowerShell 运行，或者：
```powershell
cd E:\openGhost\NousResearch-hermes-agent-4117fc3
powershell -ExecutionPolicy Bypass -File .\安装.ps1
```

**Q：pip 下载很慢**

安装前在 PowerShell 里设置国内镜像：
```powershell
$env:UV_INDEX_URL = "https://pypi.tuna.tsinghua.edu.cn/simple"
```
然后再双击 `安装.bat`

**Q：浏览器弹验证码**

用真实 Chrome 模式，在 Ghost 里说：
```
you> 用 shell_run 启动 Chrome 调试模式：
Start-Process "C:\Program Files\Google\Chrome\Application\chrome.exe" -ArgumentList "--remote-debugging-port=9222"
```
然后：
```
you> 用 code_run 连接我的真实 Chrome（CDP 端口 9222）
```

**Q：macOS 无法控制鼠标**

系统设置 → 隐私与安全性 → 辅助功能 → 添加你的终端

**Q：想切换到更快的模型**

启动后输入 `/model` 选择，推荐：
- 免费最快：Groq → `llama-3.3-70b-versatile`
- 国内稳定：阿里云 → `qwen-turbo`
- 最强能力：`deepseek-v3`
