# Ghost Agent 使用教程

> 你的数字替身 — 能自己操作电脑、浏览器、发推特，24小时自动工作

---

## 目录

1. [Windows 安装](#windows-安装)
2. [macOS 安装（苹果电脑）](#macos-安装苹果电脑)
3. [配置 API Key](#配置-api-key)
4. [启动 Ghost](#启动-ghost)
5. [让 Ghost 自动运营推特](#让-ghost-自动运营推特)
6. [设置每天自动运行](#设置每天自动运行)
7. [常用命令速查](#常用命令速查)
8. [常见问题](#常见问题)

---

## Windows 安装

### 第一步：打开 PowerShell

按 `Win + X`，选择 **Windows PowerShell** 或 **终端**

### 第二步：一行命令安装

```powershell
iex (irm https://raw.githubusercontent.com/zgbtc/ghost/master/scripts/install-ghost.ps1)
```

> 如果提示网络错误，说明 GitHub 被墙了，换用下面的方法：

**备用方法（从本地安装）：**
```powershell
cd D:\aiwork\tool\openGhost\hermes-src\NousResearch-hermes-agent-4117fc3
.\scripts\install-ghost-local.ps1
```

### 第三步：等待安装完成

安装过程大约 3-5 分钟，会看到：
```
✓ uv: uv 0.x.x
✓ Python 3.11 venv created
✓ Core dependencies installed
✓ Desktop control: pyautogui + mss + pygetwindow
✓ ghost.cmd created
```

### 第四步：重新打开 PowerShell

关掉当前 PowerShell，重新打开一个新的。

---

## macOS 安装（苹果电脑）

### Apple Silicon（M1/M2/M3/M4）和 Intel 都支持

打开 **终端（Terminal）**，运行：

```bash
curl -fsSL https://raw.githubusercontent.com/zgbtc/ghost/master/scripts/install-ghost.sh | bash
```

> 如果 GitHub 连不上，先挂 VPN 再运行

### 安装完成后重新加载终端

```bash
source ~/.zshrc
```

### macOS 必须开辅助功能权限

Ghost 要控制鼠标和键盘，需要开权限：

1. 打开 **系统设置**
2. 点 **隐私与安全性**
3. 点 **辅助功能**
4. 点右下角 **+** 号
5. 找到你的终端（Terminal 或 iTerm2），添加进去
6. 打开开关

> ⚠️ 不开这个权限，Ghost 无法控制鼠标和键盘

---

## 配置 API Key

Ghost 需要 AI 模型才能工作。推荐用国内的，速度快、免费或便宜。

### 打开配置文件

**Windows：**
```powershell
notepad $env:USERPROFILE\.hermes\.env
```

**macOS：**
```bash
nano ~/.hermes/.env
```

### 填入你的 API Key

把下面内容复制进去，填入你的 key：

```
# 必填其中一个，推荐 Groq（免费，最快）
GROQ_API_KEY=你的Groq_key

# 或者阿里云百炼（国内最稳）
DASHSCOPE_API_KEY=你的阿里云key
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Ghost 最大权限（不要删这行）
HERMES_YOLO_MODE=1
```

### 如何获取免费 API Key

**Groq（推荐，完全免费，响应 < 1秒）：**
1. 打开 https://console.groq.com
2. 注册账号
3. 点 **API Keys** → **Create API Key**
4. 复制 key，填到 `GROQ_API_KEY=` 后面

**阿里云百炼（国内，稳定）：**
1. 打开 https://bailian.console.aliyun.com
2. 注册/登录
3. 点 **API-KEY管理** → **创建API-KEY**
4. 复制 key，填到 `DASHSCOPE_API_KEY=` 后面

### 选择模型

启动 Ghost 后输入 `/model` 切换模型：

```
you> /model
```

推荐选择：
- **Groq** → `llama-3.3-70b-versatile`（免费，最快）
- **阿里云** → `qwen-turbo`（便宜，国内快）

---

## 启动 Ghost

### Windows

```powershell
ghost
```

### macOS

```bash
ghost
```

### 启动成功的样子

```
  ██████╗ ██╗  ██╗ ██████╗ ███████╗████████╗
 ██╔════╝ ██║  ██║██╔═══██╗██╔════╝╚══██╔══╝
 ...

⚠ YOLO mode — all approval prompts bypassed

you>
```

看到 `⚠ YOLO mode` 说明最大权限已开启，Ghost 可以自由操作。

看到 `you>` 就可以开始对话了。

---

## 让 Ghost 自动运营推特

### 第一步：首次登录推特（只需做一次）

```
you> 打开浏览器，去 x.com 登录，我来手动登录
```

Ghost 会打开浏览器，你手动输入账号密码登录。登录后 Cookie 会自动保存，以后不需要再登录。

### 第二步：测试基本操作

**发一条推文：**
```
you> 去 x.com 发一条推文：今天天气真好 #生活
```

**搜索并点赞：**
```
you> 去推特搜索 #AI，点赞前5条推文，每次点赞之间等待3-5秒
```

**回复推文：**
```
you> 打开这条推文 https://x.com/xxx/status/xxx，回复：Great point!
```

**查看通知：**
```
you> 打开推特通知页面，告诉我有哪些新的互动
```

### 第三步：用真实 Chrome（避免验证码）

如果遇到验证码，改用你自己的 Chrome：

**Windows：**
```
you> 用 shell_run 运行：Start-Process "C:\Program Files\Google\Chrome\Application\chrome.exe" -ArgumentList "--remote-debugging-port=9222"
```

**macOS：**
```
you> 用 shell_run 运行：open -a "Google Chrome" --args --remote-debugging-port=9222
```

Chrome 打开后：
```
you> 用 code_run 连接我的真实 Chrome（CDP 端口 9222），然后打开 x.com
```

这样 Ghost 控制的是你真实的 Chrome，有你的登录状态，不会有验证码。

---

## 设置每天自动运行

### 方法一：Cron 定时任务（推荐）

**添加每天早上9点自动发推：**
```
you> 添加一个 cron 任务，每天早上9点，搜索 #AI 最新推文，点赞前3条，然后发一条相关的推文
```

或者用命令直接添加：

**Windows：**
```powershell
ghost cron add morning-twitter "0 9 * * *" "去推特搜索 #AI，点赞前3条推文，然后发一条关于AI的推文"
```

**macOS：**
```bash
ghost cron add morning-twitter "0 9 * * *" "去推特搜索 #AI，点赞前3条推文，然后发一条关于AI的推文"
```

**查看所有定时任务：**
```
ghost cron list
```

**启动后台守护进程（让定时任务自动运行）：**

Windows：
```powershell
ghost daemon
```

macOS：
```bash
ghost daemon
```

> 让这个窗口保持开着，Ghost 就会在后台自动执行定时任务

### 方法二：Telegram 远程控制

安装后可以用手机 Telegram 控制 Ghost：

**第一步：创建 Telegram Bot**
1. 打开 Telegram，搜索 `@BotFather`
2. 发送 `/newbot`
3. 按提示设置名字，获得 Token

**第二步：配置**

编辑 `~/.hermes/.env`，加入：
```
TELEGRAM_BOT_TOKEN=你的bot_token
TELEGRAM_ALLOWED_IDS=你的Telegram用户ID
```

> 获取你的 Telegram ID：搜索 `@userinfobot`，发送任意消息

**第三步：启动**
```
ghost gateway telegram
```

之后在手机 Telegram 里给你的 Bot 发消息，Ghost 就会执行：
```
发一条推文：今天学到了新东西 #学习
```

---

## 常用命令速查

### 基本操作

| 命令 | 说明 |
|------|------|
| `ghost` | 启动 Ghost |
| `ghost setup` | 重新配置（模型、渠道等） |
| `ghost doctor` | 检查配置是否正确 |
| `ghost model` | 切换 AI 模型 |

### 在 Ghost 对话中

| 输入 | 说明 |
|------|------|
| `/model` | 切换模型 |
| `/skills` | 查看已学会的技能 |
| `/status` | 查看当前状态 |
| `/quit` 或 `Ctrl+C` | 退出 |

### 定时任务

| 命令 | 说明 |
|------|------|
| `ghost cron list` | 查看所有定时任务 |
| `ghost cron add 名字 "cron表达式" "任务描述"` | 添加定时任务 |
| `ghost daemon` | 启动后台守护进程 |

### Cron 表达式说明

```
"0 9 * * *"    每天早上9点
"0 9,18 * * *" 每天早上9点和下午6点
"0 */2 * * *"  每2小时
"0 9 * * 1"    每周一早上9点
```

---

## 常见问题

### Q：启动后没有 `⚠ YOLO mode` 提示

说明 YOLO 模式没有生效，检查 `.env` 文件：

Windows：
```powershell
Get-Content $env:USERPROFILE\.hermes\.env | Select-String "YOLO"
```

macOS：
```bash
grep YOLO ~/.hermes/.env
```

如果没有，手动添加：

Windows：
```powershell
Add-Content "$env:USERPROFILE\.hermes\.env" "HERMES_YOLO_MODE=1"
```

macOS：
```bash
echo "HERMES_YOLO_MODE=1" >> ~/.hermes/.env
```

---

### Q：Ghost 回复很慢

切换到 Groq 模型（免费，< 1秒响应）：
```
you> /model
```
选 Groq → `llama-3.3-70b-versatile`

---

### Q：浏览器弹出验证码

用真实 Chrome 模式（见上面"用真实 Chrome"章节）

---

### Q：macOS 提示"无法控制鼠标"

需要开辅助功能权限：
**系统设置 → 隐私与安全性 → 辅助功能 → 添加终端**

---

### Q：新电脑怎么安装

把你的配置文件复制过去：

1. 安装 Ghost（用上面的安装命令）
2. 复制 `.env` 文件（里面有你的 API key）
3. 复制 `config.yaml` 文件（最大权限配置）

Windows 复制命令：
```powershell
# 从旧电脑把这两个文件复制到新电脑的对应位置
# 旧电脑：C:\Users\用户名\.hermes\.env
# 新电脑：C:\Users\新用户名\.hermes\.env
```

---

### Q：想让 Ghost 学会新技能

在对话中直接教它：
```
you> 我来教你怎么在推特发带图片的推文，先截图看看当前页面...
```

Ghost 完成后会自动把这个操作保存为技能，下次直接说"发带图片的推文"就会用学到的方法。

---

## 推特自动化示例脚本

把下面的任务描述发给 Ghost，它会自动完成：

**每日推特运营任务：**
```
你好 Ghost，请帮我完成今天的推特运营：
1. 打开推特，查看通知，回复前3条提到我的推文
2. 搜索 #AI #科技，点赞最新的5条推文
3. 发一条原创推文，内容关于今天的科技新闻，加上 #AI #科技 标签
4. 关注今天互动过的2个用户
每个操作之间等待5-10秒，模拟真人操作
```

**批量互动任务：**
```
Ghost，帮我搜索关键词"人工智能"，找到最新的10条推文，
对每条推文：先看内容，如果是正面的就点赞，如果内容很好就转发，
每次操作间隔随机5-15秒
```

---

> 💡 **提示**：Ghost 会记住你的操作习惯，用得越多越聪明。每次完成复杂任务后，它会自动把操作步骤保存为技能，下次更快。
